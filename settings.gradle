plugins {
    // Auto-provisions the JDK matching the java toolchain (17) when it is not installed locally
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'demo-shop-spring-tests'
